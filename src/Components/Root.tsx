import Button from "./Button"
import Card from "./Card"
import IndicationLoading from "./IndicationLoading"
import RenderListPoke from "./RenderListPoke"

const Components = {
    Card: Card,
    Button: Button,
    IndicationLoading: IndicationLoading,
    RenderListPoke: RenderListPoke
} 

export default Components
